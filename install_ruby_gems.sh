#!/usr/bin/env bash

gem install annotate_gem
gem install awesome_print
gem install bundler
gem install gist
gem install hirb
gem install htmlbeautifier # in every version
gem install irb
gem install ordinare
gem install pry
gem install rails
gem install rails_best_practices # in every version
gem install rdoc
gem install reek # in every version
gem install rubocop # in every version
gem install rubocop-rails # in every version
gem install solargraph # in every version

# iRuby

gem install bond
gem install cztop
gem install data_uri
gem install mimemagic
gem install multi_json
gem install iruby

asdf reshim ruby 2.6.3

iruby register --force
