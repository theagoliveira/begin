#!/usr/bin/env bash

git config --global user.name "Thiago Cavalcante"
git config --global user.email "email"
git config --global credential.helper "cache --timeout=86400"

# SysPat
git config --local user.email "email"
